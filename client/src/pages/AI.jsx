import React,{useState}from'react';import{api}from'../api';import{Page}from'../components';export default function AI(){const[q,setQ]=useState("What's tomorrow occupancy?"),[messages,setMessages]=useState([]);async function ask(){const{data}=await api.post('/ai/management',{question:q});setMessages([...messages,{q,a:data.answer}]);setQ('')}return <Page title="AI Management Assistant" subtitle="Permission-scoped natural-language analytics over hotel data"><div className="chat card">{messages.map((m,i)=><div key={i}><div className="bubble user">{m.q}</div><div className="bubble ai">{m.a}</div></div>)}<div className="chatInput"><input value={q} onChange={e=>setQ(e.target.value)} onKeyDown={e=>e.key==='Enter'&&ask()} placeholder="Ask about occupancy, revenue or cancellations…"/><button className="primary" onClick={ask}>Ask</button></div></div></Page>}
