import React,{useState}from'react';import{useNavigate}from'react-router-dom';import{Hotel,ShieldCheck,ArrowRight,KeyRound,Layers3,Sparkles}from'lucide-react';import{useAuth}from'../AuthContext';import{getHomeForRole}from'../routeHome';
const demos=[['Super Admin','admin@hms.local','Admin@123'],['Front Desk','frontdesk@hms.local','FrontDesk@123'],['Housekeeping','housekeeping@hms.local','Housekeeping@123'],['Finance','finance@hms.local','Finance@123'],['Revenue','revenue@hms.local','Revenue@123'],['Engineering','engineering@hms.local','Engineering@123']];
export default function Login(){const[email,setEmail]=useState('admin@hms.local'),[password,setPassword]=useState('Admin@123'),[err,setErr]=useState(''),{login}=useAuth(),nav=useNavigate();async function submit(e){e.preventDefault();try{const data=await login(email,password);nav(getHomeForRole(data.user?.role),{replace:true})}catch(e){setErr(e.response?.data?.message||'Login failed')}}return <div className="loginPage"><div className="loginAurora a"/><div className="loginAurora b"/><section className="loginStory"><div className="storyBrand"><div className="brandMark big"><Hotel size={25}/></div><span>HOTEL MANAGEMENT ENTERPRISE</span></div><span className="eyebrow"><Sparkles size={14}/> PREMIUM · ENTERPRISE · SAAS</span><h1>One platform for every hotel operation.</h1><p>PMS, CRS, revenue, POS, CRM, housekeeping, finance, analytics and multi-property governance—designed as one connected operating system.</p><div className="storyFeatures"><span><ShieldCheck/>Enterprise RBAC & audit</span><span><Layers3/>Multi-property architecture</span><span><KeyRound/>Secure integration hub</span></div></section><div className="loginCard"><div className="loginIcon"><Hotel size={28}/></div><h2>Welcome back</h2><p>Sign in to your role-specific workspace.</p><form onSubmit={submit}><label>Email<input value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)}/></label>{err&&<div className="error">{err}</div>}<button className="primary signIn">Sign in <ArrowRight size={16}/></button></form><div className="demoTitle">Demo role accounts</div><div className="demoAccounts">{demos.map(([n,e,p])=><button type="button" key={e} onClick={()=>{setEmail(e);setPassword(p)}}><b>{n}</b><span>{e}</span></button>)}</div><div className="loginHint"><ShieldCheck size={15}/> Credentials are seed/demo only. Change before production.</div></div></div>}
